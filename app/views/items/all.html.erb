<div class="form">

</div>
<ul> <% @items.each do |i| %>
    <li><%= link_to i.name, user_item_path(i.user_id, i) %>
  <p>  : located in <%= i.user.city %> <%= i.user.state %> :</p> </li>
  <% end %></ul>
